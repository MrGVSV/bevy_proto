(
  name: "Big",
  schematics: {
    "templates::Scaled": (2.5),
    // This will have no effect since its overwritten by the one on `Player`:
    "bevy_proto::custom::SpriteBundle": (
      texture: AssetPath("textures/platformer/player/p2_front.png")
    )
  }
)